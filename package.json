{
  "name": "solana-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.6.3",
    "ts-node": "^10.9.2",
    "@types/node": "^22.9.0"
  },
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev"
  }
}
